package com.worldmind.core.model;

/**
 * Determines the level of human approval required during mission execution.
 */
public enum InteractionMode {
    FULL_AUTO,
    APPROVE_PLAN,
    STEP_BY_STEP
}
