server:
  port: ${WORLDMIND_PORT:8080}

spring:
  application:
    name: worldmind
  ai:
    anthropic:
      enabled: false
      api-key: ${ANTHROPIC_API_KEY:not-set}
      chat:
        options:
          model: claude-sonnet-4-5-20250929
          temperature: 0.0
          max-tokens: 8192
    openai:
      enabled: false
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/worldmind}
    username: ${DB_USER:worldmind}
    password: ${DB_PASSWORD:worldmind}
  jpa:
    hibernate:
      ddl-auto: validate
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
      - org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration
      - org.springframework.ai.model.anthropic.autoconfigure.AnthropicChatAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiChatAutoConfiguration
      - org.springframework.ai.model.google.genai.autoconfigure.chat.GoogleGenAiChatAutoConfiguration
      - org.springframework.ai.model.google.genai.autoconfigure.embedding.GoogleGenAiTextEmbeddingAutoConfiguration
      - org.springframework.ai.model.google.genai.autoconfigure.embedding.GoogleGenAiEmbeddingConnectionAutoConfiguration

logging:
  level:
    root: WARN
    com.worldmind: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus,metrics,info
  metrics:
    tags:
      application: worldmind
    distribution:
      percentiles-histogram:
        worldmind: true

worldmind:
  goose:
    provider: ${GOOSE_PROVIDER:openai}
    model: ${GOOSE_MODEL:qwen2.5-coder-32b}
    lm-studio-url: ${LM_STUDIO_URL:http://host.docker.internal:1234/v1}
  stargate:
    provider: ${STARGATE_PROVIDER:docker}
    max-parallel: 1
    timeout-seconds: 300
    wave-cooldown-seconds: ${WAVE_COOLDOWN_SECONDS:60}
    memory-limit-mb: 4096
    cpu-count: 2
    image: worldmind/centurion-forge:latest
  security:
    jwt:
      secret: ${WORLDMIND_JWT_SECRET:worldmind-dev-secret-change-in-production}
      expiration-seconds: 600
    command-allowlists:
      FORGE:
        - "mvn *"
        - "gradle *"
        - "npm *"
        - "pip *"
        - "python *"
        - "java *"
        - "node *"
        - "cargo *"
        - "go *"
        - "make *"
      GAUNTLET:
        - "mvn test*"
        - "gradle test*"
        - "npm test*"
        - "pytest*"
        - "python -m pytest*"
        - "java -jar *"
        - "go test*"
        - "cargo test*"
      VIGIL:
        - "find *"
        - "grep *"
        - "cat *"
        - "head *"
        - "tail *"
        - "wc *"
        - "ls *"
        - "tree *"
      PULSE:
        - "find *"
        - "grep *"
        - "cat *"
        - "head *"
        - "tail *"
        - "ls *"
      PRISM:
        - "mvn *"
        - "gradle *"
        - "npm *"
        - "pytest*"
        - "python -m pytest*"
        - "go test*"
    path-restrictions:
      FORGE:
        writable:
          - "src/**"
          - "lib/**"
          - "app/**"
          - "pkg/**"
        read-only:
          - "**"
      GAUNTLET:
        writable:
          - "src/test/**"
          - "test/**"
          - "tests/**"
          - "spec/**"
        read-only:
          - "**"
      VIGIL:
        writable: []
        read-only:
          - "**"
      PULSE:
        writable: []
        read-only:
          - "**"
      PRISM:
        writable:
          - "src/**"
          - "lib/**"
          - "app/**"
          - "pkg/**"
        read-only:
          - "**"
  health:
    timeout-seconds: 5
