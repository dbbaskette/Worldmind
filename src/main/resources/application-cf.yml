## Cloud Foundry profile — activated via SPRING_PROFILES_ACTIVE=cf
## java-cfenv auto-binds the PostgreSQL datasource from worldmind-postgres
## CfModelServiceProcessor maps worldmind-model credentials to Spring AI OpenAI props

spring:
  ai:
    openai:
      enabled: true
      chat:
        options:
          temperature: 0.0
          max-tokens: 8192
  autoconfigure:
    exclude:
      - org.springframework.ai.model.anthropic.autoconfigure.AnthropicChatAutoConfiguration
      - org.springframework.ai.model.google.genai.autoconfigure.chat.GoogleGenAiChatAutoConfiguration
      - org.springframework.ai.model.google.genai.autoconfigure.embedding.GoogleGenAiTextEmbeddingAutoConfiguration
      - org.springframework.ai.model.google.genai.autoconfigure.embedding.GoogleGenAiEmbeddingConnectionAutoConfiguration

worldmind:
  starblaster:
    provider: cloudfoundry
    max-parallel: 3
    wave-cooldown-seconds: 30
  cf:
    centurion-apps:
      forge: ${CENTURION_FORGE_APP:centurion-forge}
      gauntlet: ${CENTURION_GAUNTLET_APP:centurion-gauntlet}
      vigil: ${CENTURION_VIGIL_APP:centurion-vigil}
      pulse: ${CENTURION_PULSE_APP:centurion-pulse}
      prism: ${CENTURION_PRISM_APP:centurion-prism}
