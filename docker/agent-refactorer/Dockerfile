FROM ghcr.io/dbbaskette/agent-base:latest

LABEL com.worldmind.agent="refactorer"
LABEL com.worldmind.description="Refactoring agent with before/after test verification"

# Uses entrypoint.sh from base (VCAP_SERVICES parsing, profiles.yaml, SSL setup)
