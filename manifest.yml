---
applications:

  # --- Worldmind Orchestrator (Spring Boot JAR) ---
  - name: worldmind
    memory: 1G
    disk_quota: 2G
    instances: 1
    path: target/worldmind-0.1.0-SNAPSHOT.jar
    buildpacks:
      - java_buildpack_offline
    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 21.+ } }'
      SPRING_PROFILES_ACTIVE: cf
      worldmind.starblaster.provider: cloudfoundry
      worldmind.starblaster.image-registry: ((centurion-image-registry))
      worldmind.cf.api-url: ((cf-api-url))
      worldmind.cf.org: ((cf-org))
      worldmind.cf.space: ((cf-space))
      worldmind.cf.git-remote-url: ((git-remote-url))
      worldmind.cf.centurion-apps.forge: ((centurion-forge-app))
      worldmind.cf.centurion-apps.gauntlet: ((centurion-gauntlet-app))
      worldmind.cf.centurion-apps.vigil: ((centurion-vigil-app))
    services:
      - worldmind-postgres
      - worldmind-model

  # --- Centurion: Forge (code generation) ---
  - name: ((centurion-forge-app))
    docker:
      image: ((centurion-image-registry))/centurion-forge:latest
      username: ((docker-username))
    memory: 2G
    disk_quota: 4G
    instances: 0
    health-check-type: process
    no-route: true
    services:
      - worldmind-model

  # --- Centurion: Gauntlet (testing) ---
  - name: ((centurion-gauntlet-app))
    docker:
      image: ((centurion-image-registry))/centurion-gauntlet:latest
      username: ((docker-username))
    memory: 2G
    disk_quota: 4G
    instances: 0
    health-check-type: process
    no-route: true
    services:
      - worldmind-model

  # --- Centurion: Vigil (code review) ---
  - name: ((centurion-vigil-app))
    docker:
      image: ((centurion-image-registry))/centurion-vigil:latest
      username: ((docker-username))
    memory: 2G
    disk_quota: 4G
    instances: 0
    health-check-type: process
    no-route: true
    services:
      - worldmind-model
